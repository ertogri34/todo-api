/**
 * Type alias representing the possible statuses for API responses.
 *
 * This type is used to indicate the outcome of an operation,
 * helping to standardize the status format throughout the application.
 */
export type Status = "fail" | "error";
